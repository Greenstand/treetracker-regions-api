apiVersion: v1
kind: Service
metadata:
  name: treetracker-regions-api
spec:
  selector:
    app: treetracker-regions-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3006
